# Unicorn Run v0.5.5 patch

Changes:
- Added `index.html` as the active/base game file.
- Updated `manifest.json` to launch `./index.html`.
- Added `README.md` with project overview, controls, install instructions, file notes, credits and privacy/storage notes.
- Added `CHANGELOG.md` with the known version history.
- Updated active title/version text in `index.html` to v0.5.5.

Notes:
- No gameplay, sprite, icon, audio, score, leaderboard or touch-control behavior was intentionally changed.
